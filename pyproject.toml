[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]

[dependency-groups]
dev = [
  "debugpy>=1.8.14,<2",
  "isort>=6.0.1,<7",
  "ab-test-fixtures>=0.1.5,<0.2.0",
  "pre-commit>=4.2.0,<5",
  "pytest-asyncio>=1.4.0,<2",
  "pytest-cov>=7.1.0,<8",
  "pytest>=8.4.1,<9",
  "ruff>=0.12.3,<0.17",
  "uvicorn>=0.46.0",
  "fastapi>=0.136.1",
  "sqlmodel>=0.0.24",
]

[project]
authors = [{email = "mattcoul7@gmail.com", name = "Matt Coulter"}]
dependencies = [
    "generic-preserver>=0.1.6",
    "pydantic>=2",
]
description = "Python Pydantic support of TypeScript-style utility types, including Partial, Required, Pick, and Omit. Useful for PATCH endpoints driven from BaseModel / SQLModel classes."
name = "pydantic-patch"
readme = "README.md"
requires-python = ">=3.12,<4.0"
version = "1.5.1"

[project.optional-dependencies]
orm = [
  "sqlalchemy>=2,<3"
]

[tool.hatch.build.targets.sdist]
include = ["src/pydantic_patch"]

[tool.hatch.build.targets.wheel]
packages = ["src/pydantic_patch"]

[tool.pytest.ini_options]
asyncio_default_fixture_loop_scope = "session"
asyncio_mode = "auto"
markers = [
  "integration: Assigns the test to the integration test suite.",
  "regression: Assigns the test to the regression test suite"
]

[tool.ruff]
line-length = 120
src = ["src"]
target-version = "py312"

[tool.ruff.lint]
exclude = [".git", ".venv", "__pycache__", "proto"]
fixable = ["ALL"]
ignore = [
  "D104", # missing docstring in public package
  "UP047" # keep TypeVar-style generic functions for Python 3.12 readability
]
select = [
  "ARG001", # unused arguments
  "B", # flake8-bugbear
  "C4", # flake8-comprehensions
  "D", # pydocstyle (docstring checks)
  "E", # pycodestyle errors
  "F", # pyflakes
  "I", # isort
  "UP", # pyupgrade
  "W" # pycodestyle warnings
]

[tool.ruff.lint.per-file-ignores]
"tests/**/*.py" = ["D100", "D101", "D103", "F821"]
"src/pydantic_patch/examples/**/*.py" = ["D100", "D101", "D103", "F821"]
